import React from 'react';

const Layout = () => {
  // Shared among all the page in posts directory including [postID] i.e all the posts, new, and page.js
  return <div>Scroll to the top</div>;
};

export default Layout;
